# Packages installed into a virtualenv for the RAG pipeline.
# Models (Ollama) and the ParadeDB container are NOT pinned here -- see
# requirements.md for the full prerequisite list.

# --- LLM + embedding clients (Ollama wiring) ---
ollama>=0.3
llama-index-core>=0.12
llama-index-llms-ollama>=0.3
llama-index-embeddings-ollama>=0.5
llama-index>=0.12

# --- Postgres / vector store driver ---
psycopg[binary]>=3.1

# --- Cross-encoder reranker ---
sentence-transformers>=3.0
torch>=2.0

# --- RAGAS evaluation stack ---
ragas==0.2.7
langchain-ollama>=0.2
langchain-community>=0.3

# --- UI ---
streamlit>=1.30

# --- Tests ---
pytest>=8.0
